1.  `get_combined_dataset.ipynb`: to create the combined dataset from multiple sources and add dataset and index fields.

2. `grader.py` has the parsing and the eval logic

3. `eval.ipynb`: to run the evaluation and generate the final results.
    - we also generate the dummy student dataset here for demonstration purposes.

4. `eval_results` on what we are saving for the students after evaluation.